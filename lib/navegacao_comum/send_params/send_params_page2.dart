import 'package:flutter/material.dart';

class SendParamsPage2 extends StatelessWidget {
  const SendParamsPage2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Page2'),
      ),
      body: Container(),
    );
  }
}
