import 'package:flutter/material.dart';

class AwaitParamsPage3 extends StatelessWidget {
  const AwaitParamsPage3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Page3'),
      ),
      body: Container(),
    );
  }
}
