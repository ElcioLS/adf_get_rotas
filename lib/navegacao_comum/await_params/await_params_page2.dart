import 'package:flutter/material.dart';

class AwaitParamsPage2 extends StatelessWidget {
  const AwaitParamsPage2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Page2'),
      ),
      body: Container(),
    );
  }
}
